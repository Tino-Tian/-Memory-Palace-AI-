# /宫殿 新建类目 <路径>

手动创建新类目。

```bash
sqlite3 ~/memory-palace/palace.db "
INSERT INTO categories (parent_id, name, path, is_preset)
VALUES (
  (SELECT id FROM categories WHERE path='父级路径'),
  '新类目名称',
  '完整路径',
  0
);
"
```

注意：需要用户确认父级路径和新类目名称。如果用户在命令中给出了完整路径（如 `/宫殿 新建类目 工作/设计/插画`），自动解析父级和名称。
