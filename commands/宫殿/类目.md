# /宫殿 类目 <路径>

查看某个类目下的所有记忆条目。

```bash
sqlite3 ~/memory-palace/palace.db "
SELECT date, level, content FROM entries
WHERE category_id = (SELECT id FROM categories WHERE path='$1')
ORDER BY date DESC;
"
```

把结果整理给用户。
